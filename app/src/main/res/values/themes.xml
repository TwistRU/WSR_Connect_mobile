<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WSR_Connect_mobile" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_input</item>
        <item name="colorPrimaryVariant">@color/blue_input</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/blue_input</item>
        <item name="colorSecondaryVariant">@color/blue_input</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/blue_status_bar</item>
        <!-- Customize your theme here. -->
        <item name="textAllCaps">false</item>
        <item name="android:windowBackground">@color/blue_light</item>
        <item name="actionMenuTextColor">@color/blue_light</item>
        <item name="android:actionMenuTextColor">@color/blue_light</item>

        <item name="alertDialogTheme">@style/DialogTheme</item>
        <item name="android:alertDialogTheme">@style/DialogTheme</item>
        <item name="dialogTheme">@style/DialogTheme</item>
        <item name="android:dialogTheme">@style/DialogTheme</item>

        <item name="bottomNavigationStyle">@style/Widget.MaterialComponents.BottomNavigationView.Colored</item>
    </style>
</resources>